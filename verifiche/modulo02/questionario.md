Verifica Modulo 2: Linguaggi di Programmazione
=
Nome e Cognome: _______________________________________Data: ___________

PARTE 1: Storia e Fondamenti (Risposta Chiusa)
===
1. Qual è la differenza principale tra un linguaggio compilato (es. C) e uno interpretato (es. Python)?

    A) Il linguaggio compilato è più facile da leggere per l'essere umano.

    B) Il compilato traduce l'intero codice in linguaggio macchina prima dell'esecuzione; l'interpretato lo traduce riga per riga durante l'esecuzione.

    C) Il linguaggio interpretato non può gestire errori di sintassi.

    D) Non c'è differenza, sono sinonimi.

2. Perché il linguaggio Java è definito "portabile" (Write Once, Run Anywhere)?

    A) Perché il codice sorgente è identico all'inglese.

    B) Perché viene compilato in un formato intermedio chiamato Bytecode, eseguito da una Virtual Machine (JVM).

    C) Perché non richiede l'uso di variabili.

    D) Perché può essere eseguito solo su sistemi Linux.

3. Qual è la caratteristica distintiva della sintassi di Python rispetto a linguaggi come Java o C++?

    A) L'uso obbligatorio del punto e virgola ; a fine riga.

    B) L'uso di parentesi graffe {} per definire ogni blocco di codice.

    C) L'uso dell'indentazione (spazi a inizio riga) per definire la struttura dei blocchi.

    D) L'impossibilità di usare funzioni.

4. Il linguaggio Assembly è considerato:

    A) Un linguaggio ad alto livello, molto vicino al linguaggio umano.

    B) Un linguaggio orientato esclusivamente al web.

    C) Un linguaggio a basso livello, molto vicino all'hardware della macchina.

    D) Un linguaggio nato dopo il 2010.

PARTE 2: Programmazione a Oggetti (OOP)
====

5. In OOP, che cos'è una "Classe"?

    A) Un errore del compilatore.

    B) Lo "stampo" o progetto (blueprint) che definisce dati e comportamenti di un oggetto.

    C) Una variabile che contiene solo numeri interi.

    D) La riga di codice che chiude il programma.

6. Qual è lo scopo del metodo "Costruttore" (es. __init__ in Python)?

    A) Distruggere l'oggetto quando non serve più.

    B) Stampare a video il codice sorgente.

    C) Inizializzare gli attributi dell'oggetto nel momento in cui viene creato.

    D) Collegare il programma a Internet.

7. L'Ereditarietà permette a una classe "figlia" di:

    A) Acquisire attributi e metodi della classe "padre", potendo aggiungerne di nuovi o modificarli.

    B) Cancellare il codice della classe "padre".

    C) Funzionare solo se il computer ha due processori.

    D) Essere usata solo all'interno di cicli while.

8. Che cos'è il Polimorfismo?

    A) La capacità di un programma di cambiare linguaggio durante l'esecuzione.

    B) La possibilità per oggetti diversi di rispondere allo stesso metodo in modi differenti.

    C) Un sistema per comprimere i file di testo.

    D) L'atto di scrivere codice in più file contemporaneamente.

PARTE 3: Logica, Collezioni ed Errori
====
9. Quale struttura dati useresti per memorizzare una coppia "Chiave-Valore" (es. ID Utente -> Nome Utente)?

    A) Una Lista (Array).

    B) Una variabile di tipo Boolean.

    C) Un file eseguibile.

    D) Un Dizionario (Mappa).

10. A cosa serve il blocco try...except (o try...catch)?

    A) A velocizzare l'esecuzione del codice.

    B) A gestire le eccezioni (errori runtime) evitando che il programma crashi improvvisamente.

    C) A ripetere una porzione di codice per numerose volte.

    D) A nascondere il codice agli altri programmatori.

11. La "Persistenza dei dati" si ottiene principalmente tramite:

    A) Il salvataggio delle informazioni su supporti esterni (file, database).

    B) L'uso di variabili globali.

    C) L'aumento della memoria RAM.

    D) L'uso di commenti nel codice.

PARTE 4: Risposte Aperte (Spiegazione e Analisi)
===
12. Spiega, con una metafora del mondo reale, la differenza tra Classe e Oggetto.

13. Descrivi il concetto di Incapsulamento e spiega perché è utile nascondere alcuni dati all'interno di un oggetto.

14. Qual è la differenza tra Overriding e Overloading di un metodo?

15. Nel progetto TaMaSy, abbiamo creato le classi BugTask e FeatureTask come sottoclassi di Task. Spiega quali vantaggi ha portato questa scelta a livello di codice.

16. Perché è importante utilizzare un "Gestore di Pacchetti" (come pip o Maven) invece di copiare manualmente il codice delle librerie esterne?

17. Descrivi brevemente cosa accade quando il programma TaMaSy grafico carica i dati dal file di testo all'avvio.

18. Che cos'è la "Programmazione Modulare" e quali vantaggi offre in un progetto di grandi dimensioni?

19. Spiega la differenza tra un errore di sintassi e un errore di runtime.

20. In una GUI (Interfaccia Grafica), che cos'è il "Main Loop" (Ciclo principale)?


